require 'transaction_log'

describe Transaction_log do
  
  # it 'is displays the date of the transaction' do
  #   expeect(subject.display).to eq "#{Time.new.strftime("%d/%m/%y)} || || || "
  # end  
  
  


end